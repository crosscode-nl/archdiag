<div style="font-size: 12px; color: var(--text-muted); padding: 12px 16px; border-radius: var(--radius-card); background: var(--bg-card); border: 1px solid var(--border-default);{{if eq .Style "highlight"}} border-left: 3px solid var(--text-primary);{{end}} margin-top: 16px;">
  {{.Text}}
</div>
